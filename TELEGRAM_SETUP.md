# راهنمای نصب و پیکربندی Telegram Alerts

## مرحله ۱: ساخت ربات تلگرام

1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام دهید
2. دستور `/newbot` را ارسال کنید
3. نام و username برای ربات انتخاب کنید
4. توکن ربات را کپی کنید (مثال: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## مرحله ۲: دریافت Chat ID

1. به [@userinfobot](https://t.me/userinfobot) پیام دهید
2. Chat ID خود را کپی کنید (مثال: `123456789`)

## مرحله ۳: آپلود PHP Proxy (برای سرورهای ایران)

### چرا به Proxy نیاز داریم؟
تلگرام در ایران فیلتر است و سرور نمی‌تواند مستقیماً به API تلگرام متصل شود. برای حل این مشکل از یک Proxy PHP روی هاست خارجی استفاده می‌کنیم.

### مراحل نصب:

1. فایل `telegram-proxy.php` را روی هاست خارجی خود آپلود کنید
   - می‌توانید از هاست‌های رایگان مثل InfinityFree، 000webhost استفاده کنید
   - یا از سرویس‌های ابری مثل Cloudflare Workers

2. مطمئن شوید PHP و extension `curl` فعال است

3. URL فایل را یادداشت کنید (مثال: `https://yourdomain.com/telegram-proxy.php`)

## مرحله ۴: پیکربندی سرور

1. فایل `.env` را در پوشه اصلی پروژه ایجاد کنید:

```bash
cp .env.example .env
```

2. فایل `.env` را ویرایش کنید:

```env
# Telegram Bot Token (از BotFather دریافت کنید)
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Chat ID (از userinfobot دریافت کنید)
TELEGRAM_CHAT_ID=123456789

# URL فایل PHP Proxy
TELEGRAM_PROXY_URL=https://yourdomain.com/telegram-proxy.php

# آستانه‌های هشدار (درصد)
CPU_THRESHOLD=80
MEMORY_THRESHOLD=80
DISK_THRESHOLD=80
```

## مرحله ۵: راه‌اندازی

```bash
# نصب dependencies
npm install axios

# راه‌اندازی سرور
npm start
```

## تست کردن

پس از راه‌اندازی، باید یک پیام خوش‌آمدگویی در تلگرام دریافت کنید:

```
🚀 Health Monitor Started

Monitoring system resources every 15 minutes.
You will receive alerts when thresholds are exceeded.
```

## نحوه کار

- سرور هر **15 دقیقه** یک بار منابع سیستم را بررسی می‌کند
- اگر CPU، Memory یا Disk از آستانه تعیین شده بالاتر رود، هشدار ارسال می‌شود
- بین هر هشدار **15 دقیقه** فاصله است تا spam نشود

## نمونه پیام هشدار

```
🔥 CPU Alert!

⚠️ CPU usage is 85.3%
📊 Threshold: 80%
⏰ Time: 1404/10/13 01:30:45

Cores: 8
```

## عیب‌یابی

### خطا: "Telegram bot token or chat ID not configured"
- مطمئن شوید فایل `.env` در پوشه اصلی پروژه وجود دارد
- بررسی کنید که `TELEGRAM_BOT_TOKEN` و `TELEGRAM_CHAT_ID` صحیح هستند

### خطا: "Failed to send Telegram message"
- بررسی کنید که PHP Proxy به درستی آپلود شده است
- URL Proxy را در مرورگر باز کنید - باید خطای "Method not allowed" نمایش دهد
- لاگ‌های PHP را بررسی کنید

### پیام دریافت نمی‌شود
- مطمئن شوید که ربات را در تلگرام `/start` کرده‌اید
- Chat ID را دوباره از @userinfobot دریافت کنید
- بررسی کنید که هاست خارجی شما به تلگرام دسترسی دارد

## استفاده بدون Proxy (خارج از ایران)

اگر سرور شما خارج از ایران است، می‌توانید بدون Proxy استفاده کنید:

```env
TELEGRAM_BOT_TOKEN=your_token
TELEGRAM_CHAT_ID=your_chat_id
# TELEGRAM_PROXY_URL را خالی بگذارید یا کامنت کنید
```

## امنیت

- توکن ربات و Chat ID را **هرگز** در GitHub یا جای عمومی قرار ندهید
- فایل `.env` در `.gitignore` قرار دارد و commit نمی‌شود
- PHP Proxy فقط متدهای مجاز (`sendMessage`, `sendPhoto`, `sendDocument`) را اجازه می‌دهد
